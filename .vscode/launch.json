{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/src/VendaFacil.UI/package.json",
                "localRoot": "${workspaceFolder}/src/VendaFacil.UI"
            }
        }
    ]
}